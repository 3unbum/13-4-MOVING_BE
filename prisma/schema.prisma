// 무빙 Prisma 스키마
// ERD 원본: docs/message.txt (DBML)
// TODO: 12개 모델 + 7개 Enum 작성 (이슈 #20)

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}
